# ROLLBACK REPORT — v.21.0520 (20 Mei 2026)

## Keputusan
Kyai memutuskan rollback site UTAMA dari Vue 3 SPA ke Legacy single-file (index.html 41k lines).

## Yang Berubah

### A. Firebase Hosting Target Switch
| Target | Sebelum | Sesudah |
|---|---|---|
| `main` (ammuonline.web.app) | `public/vue` (Vue 3 SPA) | `public` (Legacy index.html) |
| `legacy` (portal-mambaul-ulum.web.app) | `public` (Legacy) | `public` (Legacy) — tetap |
| `psb` (ammuonline-psb.web.app) | `public/psb` | `public/psb` — tetap |

**Efek**: ammuonline.web.app sekarang serve Legacy index.html.  
Vue tetap tersimpan di `public/vue/` sebagai backup.

### B. Version Bump
- Legacy APP_VERSION: `v.20.0526` → `v.21.0520`

### C. File Integrity
- `firebase.json` — OK (9568 bytes, 0 NUL)
- `public/index.html` — OK (1,966,155 bytes = ~1.9MB, 41048 lines, 0 NUL)
- `public/vue/index.html` — OK (backup preserved)
- `public/psb/index.html` — OK

## Feature Scan: Legacy vs Vue

### ✅ SUDAH ADA di Legacy (32/40 fitur)
Legacy adalah source of truth fitur. Semua function berikut SUDAH ada:
- renderAuditLog, renderAuditIssues (Audit Log viewer)
- renderMasterTP (Master Tahun Pelajaran CRUD)
- renderBulkGuruList (Bulk Guru actions)
- renderNominalSyahriyahGrid (Tabungan nominal preset)
- renderTunjPot (Tunjangan & Potongan)
- renderChartKeuangan + Chart.js (4 chart statistik)
- renderAcfFieldsEditor (ACF custom fields)
- renderPreviewGridBP (Preview Buku Pribadi)
- renderTentangKami (About page)
- renderLightboxImg (Image zoom)
- renderStatusGoogleProfil (Google login handler)
- renderKegiatanPesantren (Kegiatan + fingerprint)
- txtHeaderBar, txtSidebarTitle, bgImage, logoKop, themeColor
- kalibrasiHijri, raporSchemas, shift configs
- Fingerprint ID column, TTD EKGQ, Absensi orphan delete
- Non-lembaga filter, TPQ shift Pagi/Sore

### ⚠️ GAP — Vue-only features belum di Legacy (3 area)

1. **PSB Admin section** — Vue punya /psb route admin (list submissions + detail). Legacy BELUM ada section PSB admin. PSB public form (ammuonline-psb.web.app) tetap jalan.
   - **Rekomendasi**: Tambah section PSB Admin di legacy sesi berikutnya. Data PSB di Firestore collection tetap bisa diakses.

2. **Feature toggles** (fiturBeranda, fiturKalender, fiturKritikSaran, fiturNotifikasi) — Vue punya toggle on/off per fitur di Pengaturan. Legacy belum.
   - **Rekomendasi**: Minor priority. Fitur ini hanya show/hide section, bisa ditambah nanti.

3. **Postingan settings** (postinganHanyaAdmin, postinganAutoApprove, postinganMaxImages, postinganMaxSizeKb) — Vue punya control posting di Pengaturan. Legacy belum.
   - **Rekomendasi**: Minor priority. Default behavior legacy sudah OK.

### Tidak di-port (alasan)
- Port code ke file 41k lines berisiko tinggi truncation (Windows mount bug sudah terdokumentasi di memory). Sebaiknya dilakukan interaktif di sesi chat langsung dengan kyai, bukan scheduled task.

## Deploy Instructions

Kyai tinggal jalankan:
```
npm run firebase:deploy
```

Setelah deploy:
- https://ammuonline.web.app → Legacy index.html (site UTAMA)
- https://portal-mambaul-ulum.web.app → Legacy index.html (tetap)
- https://ammuonline-psb.web.app → PSB public form (tetap)

## Next Steps (sesi berikutnya)
1. Tambah PSB Admin section ke legacy index.html
2. Tambah feature toggles (fiturBeranda dll) ke Pengaturan legacy
3. Tambah postingan settings ke Pengaturan legacy
4. Test login + semua fitur di ammuonline.web.app setelah deploy
5. Update Capacitor app (jika masih pakai server.url ke ammuonline.web.app, otomatis ikut rollback)

---
Generated by scheduled task: portal-mu-rollback-legacy-utama-1200
