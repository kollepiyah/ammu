B2 + Toast compact + lazy-init: Firestore Rules tightening + toast UX final

B2 - Firestore Rules tightening:
- Migrate `allow write: if true` -> `if signedIn()` (request.auth != null)
- READ tetap public untuk login lookup compatibility
- kritik_saran CREATE tetap allow anonymous
- Default deny untuk collection tidak tercantum
- Backup rules lama: firestore.rules.bak.v.108.43

Toast notification v.108.51:
- LAZY INIT _toastMixinInstance (Swal defer load fix)
- Hapus mouseenter handler (penyebab timer stuck / not auto-dismiss)
- Ukuran lebih KOMPAK: 220px min-width (was 280), padding 10px (was 14)
- Font 12px, icon 26px (smaller untuk tidak mengganggu)
- Timer progress bar 2px teal-emerald gradient
- Hide #global-loader saat Swal active (anti shadow bocor)
- Dark mode support

Skipped (untuk versi mendatang):
- B3 Palette teal-emerald (file truncation berulang, tunda)
- Custom modal Swal styling (bikin OK button stuck, revert ke default)

Bugfix:
- sw.js null bytes (332 byte) — penyebab husky prettier reject
- gitignore: tambah pola *.bak.* + commit-msg.txt

APP_VERSION: v.108.51.0513-toast-compact
SW_VERSION:  v249-0513-toast-compact
