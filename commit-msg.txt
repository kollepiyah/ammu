B0+B1+UX: Recovery + Firebase Auth migration + Toast UX

B0 - Recovery hotfixes:
- Restore truncated index.html (3x) via git HEAD stitching
- sw.js missing closing parenthesis fix
- Add _preCacheLogos helper (pre-cache logo URLs)
- Add _imgUrlCache + _cacheImgUrl (dataURL cache for jsPDF)
- Menu admin role_sistem support (admin biasa & super_admin)

B1 - Firebase Auth Hybrid Migration (5 phases):
- P1: Lazy migration login flow (Auth-first, Firestore fallback)
- P2: Auto-provision Auth on new user creation (secondary Firebase app)
- P2.1: Client rate-limit 5/5min + 2s cooldown anti too-many-requests
- P2.2: Internal password padding 'mu_auth_' bypass min-6-char Firebase rule
- P3: Self-edit password sync ke Firebase Auth (admin/guru/santri)

UX Improvements:
- Toast notification bottom-right mobile-style (194 popup -> silent toast)
- Inline login button cooldown spinner (no popup)
- Login page bocor fix (CSS ULTRA-NUKE + JS force hide app-view)
- Defensive error path: force hide app-view + signOut on initApp catch

Security:
- escapeHtml() di dropdown guru options (XSS prevention)
- cekHakAkses() guards di 9 destructive functions

Infrastructure:
- Husky pre-commit hook (block credentials commit)
- Iframely API integration for social media link preview (Cloud Function)
- Firebase Functions v2 + Secret Manager migration

APP_VERSION: v.108.42.0513-toast-notif
SW_VERSION:  v241-0513-toast-notif
