<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.DayNight.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:background">@null</item>
        <!-- v.93.0626: ikon status bar GELAP utk bg terang (mode terang) sejak native, biar teks
             status bar langsung kelihatan tanpa nunggu JS. Mode gelap di-override JS (Style.Dark). -->
        <item name="android:windowLightStatusBar">true</item>
    </style>


    <style name="AppTheme.NoActionBarLaunch" parent="Theme.SplashScreen">
        <!-- v.91.0626 IN-APP ANIM (final): splash sistem = LATAR mint singkat (ikon transparan,
             TANPA logo/footer). Logo (animasi Netflix 2 detik) + footer "Powered by Bakafrawi"
             (gambar HTML, anti-gepeng) dirender di DALAM app. Slot branding sistem TIDAK dipakai
             karena men-stretch footer (gepeng) di sebagian HP termasuk Samsung. -->
        <item name="windowSplashScreenBackground">@color/splashBg</item>
        <item name="windowSplashScreenAnimatedIcon">@drawable/splash_blank</item>
        <item name="postSplashScreenTheme">@style/AppTheme.NoActionBar</item>
    </style>
</resources>